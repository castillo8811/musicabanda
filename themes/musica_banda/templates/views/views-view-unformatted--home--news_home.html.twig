<div id="last-news">
  <div class="block PN3r0 text-bold ml10 text-upper">
    <span class="text-yellow">Últimas </span><span class="text-gray">Noticias</span>
  </div>
  {% for row in rows %}
    {%
    set row_classes = [
    default_row_class ? 'views-row',
    'panel',
    'panel-default',
    ]
    %}

    <div class="nodeListItem col-lg-4 col-xs-6">
      {{ row.content }}
    </div>
  {% endfor %}
</div>