<div id="stage">
  {% for row in rows %}
    {%
    set row_classes = [
    default_row_class ? 'views-row',
    'panel',
    'panel-default',
    ]
    %}

    <div class="stage-item col-lg-4">
      {{ row.content }}
    </div>
  {% endfor %}
</div>